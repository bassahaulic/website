rules_version = '2';

// M0: deny everything. Real role- and organization-scoped rules land in M1,
// together with emulator permission tests.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
