import { APP_NAME } from "@haulpilot/shared";

export default function HomePage() {
  return (
    <main className="shell">
      <h1>{APP_NAME} Dispatch</h1>
      <p>Application shell — M0. Product screens arrive in later milestones.</p>
    </main>
  );
}
